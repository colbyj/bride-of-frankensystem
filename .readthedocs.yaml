version: "2"

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.14"
  apt_packages:
    - librsvg2-bin

python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/conf.py

formats:
  - pdf
